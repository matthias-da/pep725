% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_normals.R
\name{plot.pheno_normals}
\alias{plot.pheno_normals}
\title{Plot Method for Phenological Normals}
\usage{
\method{plot}{pheno_normals}(x, which = c("dotplot", "bar"), ...)
}
\arguments{
\item{x}{A \code{pheno_normals} object returned by \code{\link{pheno_normals}}.}

\item{which}{Character string selecting the plot type: \code{"dotplot"}
(default) shows \code{median_doy} with Q25--Q75 range via
\code{geom_pointrange}; \code{"bar"} shows \code{median_doy} as columns
with error bars.}

\item{...}{Additional arguments (unused).}
}
\value{
A \code{ggplot} object (returned invisibly).
}
\description{
Visualise phenological normals as a dot plot with interquartile-range bars
or as a bar chart.
}
\examples{
\donttest{
pep <- pep_download()
pep_ch <- pep[country == "Switzerland"]

# Normals by phase for apple
n <- pheno_normals(pep_ch, species = "Malus", by = "phase_id")
plot(n)
plot(n, which = "bar")
}

}
\seealso{
\code{\link{pheno_normals}}, \code{\link{pheno_anomaly}}
}
\author{
Matthias Templ
}
