% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pheno_timeseries.R
\name{pheno_plot_timeseries}
\alias{pheno_plot_timeseries}
\title{Plot Phenological Time Series}
\usage{
pheno_plot_timeseries(
  data,
  color_by = "species",
  facet_by = NULL,
  alpha_lines = 0.7,
  linewidth = 0.8,
  smooth = TRUE,
  se = FALSE,
  year_min = 1961,
  title = "Phenological time series of flowering (BBCH 65)"
)
}
\arguments{
\item{data}{A data.frame or data.table containing at least \code{year}, \code{DOY}, and one grouping variable
(e.g. \code{species}, \code{phase}, or \code{functional_group}).}

\item{color_by}{Character. Column name used for color grouping (default = "species").}

\item{facet_by}{Character or NULL. Optional column name for faceting (e.g. "functional_group").}

\item{alpha_lines}{Numeric. Transparency level for time-series lines (default = 0.7).}

\item{linewidth}{Numeric. Line width (default = 0.8).}

\item{smooth}{Logical. If TRUE, adds a linear trend smoother (default = TRUE).}

\item{se}{Logical. Whether to display confidence interval for smoother (default = FALSE).}

\item{year_min}{Numeric. Minimum year to plot (default = 1961).}

\item{title}{Character. Optional plot title.}
}
\value{
A ggplot object.
}
\description{
Visualizes phenological day-of-year (DOY) trends over time since 1961.
Designed for aggregated or site-level phenology data from PEP725.
}
\examples{
\donttest{
pep <- pep_download()
# Example: aggregated flowering DOY by species
pheno_plot_timeseries(data = pep, color_by = "functional_group",
                      facet_by = NULL, smooth = TRUE)

# Example 1: all species, colored by functional group
pheno_plot_timeseries(
  data = pep[phase_id == 65],
  color_by = "functional_group",
  smooth = TRUE,
  title = "Flowering (BBCH 65) time series since 1961"
)

# Example 2: single species, colored by site
pheno_plot_timeseries(
  data = pep[species == "Triticum aestivum" & phase_id == 65],
  color_by = "s_id",
  smooth = TRUE,
  title = "Winter wheat flowering (BBCH 65) across sites"
)

# Example 3: facets by functional group
pheno_plot_timeseries(
  data = pep[phase_id == 65],
  color_by = "species",
  facet_by = "functional_group",
  smooth = TRUE
)
}
}
\author{
Matthias Templ
}
